#include <stdio.h>

int main(){

    printf("hello, world\n");
    printf("this is a test, using the backslash c, \c");
    // note that the backslash c does not work in the terminal, \c isn't recognized because it's not a valid escape sequence
    return 0;
}